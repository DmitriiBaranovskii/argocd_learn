apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonAnnotations:
  app: nginx-app


labels:
  - pairs:
      app: nginx-app
    includeTemplates: true
    includeSelectors: true
